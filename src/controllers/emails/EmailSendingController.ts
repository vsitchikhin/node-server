import { IResponse } from 'types/api/api.types';
import { PrismaClient, Users } from '@prisma/client';
import { ErrorsTypesEnum } from 'types/errorsTypesEnum';

export async function sendEmail(payload: EmailSenderApi) {}